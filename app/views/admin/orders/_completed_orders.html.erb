<div id="tabs8" class="default-tabs" style="display:<%= params[:tab] == "tabs8" ? 'block' : 'none' %>">
  <table width="100%" border="0" cellspacing="10" cellpadding="10">
    <tr class="dark-light-green">
      <td>NF#</td>
      <td>SIGNER</td>
      <td>ORDER OPENED</td>
      <td>APPOINTMENT SCHEDULED</td>
      <td>SIGNING LOCATION</td>
      <td>CLIENT NAME</td>
      <td>MOVE TO PAID</td>
    </tr>
    <% @completed_orders.each do |order| %>
      <tr class='<%= order_status_class(order) %>'>
        <%= admin_order_table_data(order, 'tabs8') %>
      </tr>
    <% end %>
    <% if @completed_orders.blank? %>
      <tr class="dark-light-green">
        <td colspan='7' style="text-align: center">You currently have no completed orders.</td>
      </tr>
    <% end %>
  </table>
</div>
