<h2>New Executive</h2>
<% if flash[:errors].present? %>
				<div class="flash error" style="margin-top:10px;">
								<ul>
												<% flash[:errors].each {|e| %>
																<li><%= e %> </li>
												<% } %>
												<% flash[:errors]=nil %>
								</ul>
				</div>
<% end %>
<% form_for :executive, :url => "create", :html => {:multipart => true,:name=>"new_executive_form"} do |f| -%>
				<%= render :partial => 'form', :locals => {:f => f, :button_img => 'save_profile.png' }%>
<% end %>
